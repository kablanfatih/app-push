server:
  port: 8881

spring:
   datasource:
     url: jdbc:postgresql://localhost:5432/apppush
     username: postgres
     password: postgres
jpa:
  database: postgresql
  show-sql: true
  hibernate:
    ddl-auto: update
    open-in-view: false

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/